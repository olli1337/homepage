language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules
script:
  - npm test
  - npm run build
deploy:
- provider: script
  script: git fetch --unshallow && git push https://$AZURE_WA_USERNAME:$AZURE_WA_PASSWORD@havilehto.scm.azurewebsites.net:443/havilehto.git HEAD:refs/heads/master
  on: master
